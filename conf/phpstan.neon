includes:
    - ./phpstan-baseline.neon

parameters:

    paths:
         - ../app
         - ../packages

    # Level 9 is the highest level
    level: 5

rules:
    - Drmovi\PackageBoundaries\PackageBoundaries
